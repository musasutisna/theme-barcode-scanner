import { CameraDecoderDirective } from './camera-decoder.directive';

describe('CameraDecoderDirective', () => {
  it('should create an instance', () => {
    const directive = new CameraDecoderDirective();
    expect(directive).toBeTruthy();
  });
});
