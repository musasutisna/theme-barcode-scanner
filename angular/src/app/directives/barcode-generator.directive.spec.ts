import { BarcodeGeneratorDirective } from './barcode-generator.directive';

describe('BarcodeGeneratorDirective', () => {
  it('should create an instance', () => {
    const directive = new BarcodeGeneratorDirective();
    expect(directive).toBeTruthy();
  });
});
